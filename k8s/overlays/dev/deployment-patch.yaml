apiVersion: apps/v1
kind: Deployment
metadata:
  name: onboarding-app
spec:
  template:
    spec:
      containers:
        - name: onboarding
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
